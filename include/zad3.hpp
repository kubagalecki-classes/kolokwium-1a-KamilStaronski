#include "zad1.hpp"
#include <cstdint>
#include <functional>

// tutaj funkcja polejSosem
template <typename typ>
std::size_t polejSosem(const Tagiatelle& t, const typ &sos) {
	unsigned double wynik=polej(sos,t)
	return wynik;
}
